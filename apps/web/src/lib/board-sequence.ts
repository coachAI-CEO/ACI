/**
 * Web re-export of @aci/shared board-sequence.
 *
 * Canonical source: `packages/shared/src/board/sequence.ts`.
 * This file is kept so the existing `from "@/lib/board-sequence"`
 * imports across the web app keep working without a global rename.
 */
export * from "@aci/shared";
