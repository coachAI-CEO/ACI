/**
 * Web re-export of @aci/shared board-player-spacing.
 *
 * Canonical source: `packages/shared/src/board/player-spacing.ts`.
 * This file is kept so the existing `from "@/lib/board-player-spacing"`
 * imports across the web app keep working without a global rename.
 */
export * from "@aci/shared";
