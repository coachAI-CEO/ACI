/**
 * Web re-export of @aci/shared pitch-formats.
 *
 * The canonical source is `packages/shared/src/board/pitch-formats.ts`.
 * This file is kept so the existing `from "@/lib/pitch-formats"` imports
 * across the web app keep working without a global rename.
 */
export * from "@aci/shared";
