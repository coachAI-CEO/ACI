/**
 * Web re-export of @aci/shared board-lines.
 *
 * Canonical source: `packages/shared/src/board/lines.ts`.
 * This file is kept so the existing `from "@/lib/board-lines"` imports
 * across the web app keep working without a global rename.
 */
export * from "@aci/shared";
