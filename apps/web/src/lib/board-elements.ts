/**
 * Web re-export of @aci/shared board-elements.
 *
 * Canonical source: `packages/shared/src/board/elements.ts`.
 * This file is kept so the existing `from "@/lib/board-elements"` imports
 * across the web app keep working without a global rename.
 */
export * from "@aci/shared";
